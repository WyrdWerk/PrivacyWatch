# Required only for npm run deploy:legacy-redirect (pre-migration Cloudflare Pages project name).
LEGACY_PAGES_PROJECT=

# Optional override for redirect target (defaults to https://policywatch.wyrdwerk.com).
# POLICYWATCH_URL=
