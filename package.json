{
  "name": "modelwatch",
  "version": "1.3.0",
  "private": true,
  "description": "LLM Data Policy Tracker — track data retention and model training policies",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && npm run validate && wrangler pages deploy dist --project-name modelwatch",
    "dev": "npm run build && wrangler pages dev dist",
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
